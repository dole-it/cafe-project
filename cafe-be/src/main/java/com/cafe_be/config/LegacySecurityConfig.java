package com.cafe_be.config;

// This class was kept for historical reasons but should not be registered as a configuration bean.
// The active security configuration lives in com.cafe_be.security.SecurityConfig
public class LegacySecurityConfig {
    // intentionally empty
}
